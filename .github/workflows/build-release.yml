name: Build Release APK

on:
  workflow_dispatch:
    inputs:
      version_name:
        description: '版本名称 (例如: 1.0.0)'
        required: false
        default: '1.0.0'
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout代码
        uses: actions/checkout@v4
      
      - name: 设置JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle
      
      - name: 设置Gradle
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.4
      
      - name: 生成Gradle Wrapper
        run: gradle wrapper --gradle-version 8.4
      
      - name: 授予Gradle执行权限
        run: chmod +x gradlew
      
      - name: 清理构建
        run: ./gradlew clean
      
      - name: 构建Release APK
        run: ./gradlew assembleRelease
        env:
          VERSION_NAME: ${{ github.event.inputs.version_name }}
      
      - name: 签名APK (Debug签名)
        run: |
          # 如果没有配置release签名，使用debug签名
          echo "使用Debug签名构建APK"
      
      - name: 重命名APK
        run: |
          VERSION="${{ github.event.inputs.version_name }}"
          TIMESTAMP=$(date +%Y%m%d_%H%M%S)
          cd app/build/outputs/apk/release
          for file in *.apk; do
            if [ -f "$file" ]; then
              mv "$file" "CellularRouter-v${VERSION}-${TIMESTAMP}.apk"
            fi
          done
      
      - name: 上传APK产物
        uses: actions/upload-artifact@v4
        with:
          name: CellularRouter-Release-APK
          path: app/build/outputs/apk/release/*.apk
          retention-days: 30
      
      - name: 获取APK文件名
        id: get-apk
        run: |
          APK_PATH=$(ls app/build/outputs/apk/release/*.apk | head -n 1)
          APK_NAME=$(basename "$APK_PATH")
          echo "apk_path=$APK_PATH" >> $GITHUB_OUTPUT
          echo "apk_name=$APK_NAME" >> $GITHUB_OUTPUT
      
      - name: 创建GitHub Release
        id: create-release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ github.event.inputs.version_name }}
          release_name: Release v${{ github.event.inputs.version_name }}
          body: |
            ## CellularRouter v${{ github.event.inputs.version_name }}
            
            ### 📦 下载
            下载下方的APK文件安装到Android设备
            
            ### ✨ 功能特性
            - 统一端口SOCKS5+HTTP代理
            - 自动协议检测
            - 蜂窝网络强制路由
            - 实时流量统计
            - Material Design 3界面
            
            ### 📋 系统要求
            - Android 6.0 (API 23) 或更高版本
            - 设备需支持蜂窝网络
            
            ### 🔧 构建信息
            - 构建时间: ${{ github.run_id }}
            - Commit: ${{ github.sha }}
            - 分支: ${{ github.ref_name }}
          draft: false
          prerelease: false
      
      - name: 上传APK到Release
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create-release.outputs.upload_url }}
          asset_path: ${{ steps.get-apk.outputs.apk_path }}
          asset_name: ${{ steps.get-apk.outputs.apk_name }}
          asset_content_type: application/vnd.android.package-archive
      
      - name: 生成构建信息
        run: |
          echo "### 构建完成 ✅" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**版本**: ${{ github.event.inputs.version_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Release**: [v${{ github.event.inputs.version_name }}](${{ steps.create-release.outputs.html_url }})" >> $GITHUB_STEP_SUMMARY
          echo "**构建时间**: $(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          echo "**分支**: ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Commit**: ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "✅ APK已发布到GitHub Release" >> $GITHUB_STEP_SUMMARY
